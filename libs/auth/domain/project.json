{
  "name": "auth-domain",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "libs/auth/domain/src",
  "prefix": "lib",
  "projectType": "library",
  "tags": ["type:domain"],
  "targets": {
    "test": {
      "executor": "@nx/vite:test",
      "outputs": ["{options.reportsDirectory}"],
      "options": {
        "reportsDirectory": "../../../coverage/libs/auth/domain"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint"
    }
  }
}
